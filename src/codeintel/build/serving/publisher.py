"""Deprecated serving snapshot publisher shim.

Use `codeintel.serving.publisher` instead.
"""

from __future__ import annotations

msg = "codeintel.build.serving.publisher moved to codeintel.serving.publisher"
raise ImportError(msg)
