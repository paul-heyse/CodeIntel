<?xml version="1.0" encoding="utf-8"?><testsuites name="pytest tests"><testsuite name="pytest" errors="141" failures="0" skipped="1" tests="142" time="32.401" timestamp="2025-12-24T05:34:23.001850-06:00" hostname="MacBookPro.lan"><testcase classname="tests.serving.test_semantic_http_routes" name="test_semantic_routes_support_correlation_id" time="0.009"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">fixturedef = &lt;FixtureDef argname='require_tooling' scope='session' baseid='tests'&gt;
request = &lt;SubRequest 'require_tooling' for &lt;Function test_semantic_routes_support_correlation_id&gt;&gt;

    @pytest.hookimpl(wrapper=True)
    def pytest_fixture_setup(fixturedef: FixtureDef, request) -&gt; object | None:
        asyncio_mode = _get_asyncio_mode(request.config)
        if not _is_asyncio_fixture_function(fixturedef.func):
            if asyncio_mode == Mode.STRICT:
                # Ignore async fixtures without explicit asyncio mark in strict mode
                # This applies to pytest_trio fixtures, for example
                return (yield)
            if not _is_coroutine_or_asyncgen(fixturedef.func):
&gt;               return (yield)
                        ^^^^^

.venv/lib/python3.13/site-packages/pytest_asyncio/plugin.py:730: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/conftest.py:246: in require_tooling
    _require_tooling()
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.serving.test_semantic_http_routes" name="test_semantic_routes_end_to_end" time="0.014"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">fixturedef = &lt;FixtureDef argname='require_tooling' scope='session' baseid='tests'&gt;
request = &lt;SubRequest 'require_tooling' for &lt;Function test_semantic_routes_end_to_end&gt;&gt;

    @pytest.hookimpl(wrapper=True)
    def pytest_fixture_setup(fixturedef: FixtureDef, request) -&gt; object | None:
        asyncio_mode = _get_asyncio_mode(request.config)
        if not _is_asyncio_fixture_function(fixturedef.func):
            if asyncio_mode == Mode.STRICT:
                # Ignore async fixtures without explicit asyncio mark in strict mode
                # This applies to pytest_trio fixtures, for example
                return (yield)
            if not _is_coroutine_or_asyncgen(fixturedef.func):
&gt;               return (yield)
                        ^^^^^

.venv/lib/python3.13/site-packages/pytest_asyncio/plugin.py:730: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/conftest.py:246: in require_tooling
    _require_tooling()
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.serving.test_semantic_mcp_tools" name="test_mcp_serving_meta_typed_response[asyncio]" time="0.008"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">self = &lt;Coroutine test_mcp_serving_meta_typed_response[asyncio]&gt;

    def setup(self) -&gt; None:
        runner_fixture_id = f"_{self._loop_scope}_scoped_runner"
        if runner_fixture_id not in self.fixturenames:
            self.fixturenames.append(runner_fixture_id)
&gt;       return super().setup()
               ^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/pytest_asyncio/plugin.py:458: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
.venv/lib/python3.13/site-packages/pytest_asyncio/plugin.py:730: in pytest_fixture_setup
    return (yield)
            ^^^^^
tests/conftest.py:246: in require_tooling
    _require_tooling()
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.serving.test_semantic_mcp_tools" name="test_mcp_tool_annotations_present[asyncio]" time="0.009"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">self = &lt;Coroutine test_mcp_tool_annotations_present[asyncio]&gt;

    def setup(self) -&gt; None:
        runner_fixture_id = f"_{self._loop_scope}_scoped_runner"
        if runner_fixture_id not in self.fixturenames:
            self.fixturenames.append(runner_fixture_id)
&gt;       return super().setup()
               ^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/pytest_asyncio/plugin.py:458: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
.venv/lib/python3.13/site-packages/pytest_asyncio/plugin.py:730: in pytest_fixture_setup
    return (yield)
            ^^^^^
tests/conftest.py:246: in require_tooling
    _require_tooling()
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.serving.test_semantic_http_routes" name="test_semantic_route_invalid_filter_returns_400" time="0.010"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">fixturedef = &lt;FixtureDef argname='require_tooling' scope='session' baseid='tests'&gt;
request = &lt;SubRequest 'require_tooling' for &lt;Function test_semantic_route_invalid_filter_returns_400&gt;&gt;

    @pytest.hookimpl(wrapper=True)
    def pytest_fixture_setup(fixturedef: FixtureDef, request) -&gt; object | None:
        asyncio_mode = _get_asyncio_mode(request.config)
        if not _is_asyncio_fixture_function(fixturedef.func):
            if asyncio_mode == Mode.STRICT:
                # Ignore async fixtures without explicit asyncio mark in strict mode
                # This applies to pytest_trio fixtures, for example
                return (yield)
            if not _is_coroutine_or_asyncgen(fixturedef.func):
&gt;               return (yield)
                        ^^^^^

.venv/lib/python3.13/site-packages/pytest_asyncio/plugin.py:730: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/conftest.py:246: in require_tooling
    _require_tooling()
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.serving.test_semantic_http_routes" name="test_semantic_routes_support_optional_api_key" time="0.010"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">fixturedef = &lt;FixtureDef argname='require_tooling' scope='session' baseid='tests'&gt;
request = &lt;SubRequest 'require_tooling' for &lt;Function test_semantic_routes_support_optional_api_key&gt;&gt;

    @pytest.hookimpl(wrapper=True)
    def pytest_fixture_setup(fixturedef: FixtureDef, request) -&gt; object | None:
        asyncio_mode = _get_asyncio_mode(request.config)
        if not _is_asyncio_fixture_function(fixturedef.func):
            if asyncio_mode == Mode.STRICT:
                # Ignore async fixtures without explicit asyncio mark in strict mode
                # This applies to pytest_trio fixtures, for example
                return (yield)
            if not _is_coroutine_or_asyncgen(fixturedef.func):
&gt;               return (yield)
                        ^^^^^

.venv/lib/python3.13/site-packages/pytest_asyncio/plugin.py:730: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/conftest.py:246: in require_tooling
    _require_tooling()
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.serving.test_semantic_mcp_tools" name="test_mcp_tools_catalog_describe_and_query[asyncio]" time="0.010"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">self = &lt;Coroutine test_mcp_tools_catalog_describe_and_query[asyncio]&gt;

    def setup(self) -&gt; None:
        runner_fixture_id = f"_{self._loop_scope}_scoped_runner"
        if runner_fixture_id not in self.fixturenames:
            self.fixturenames.append(runner_fixture_id)
&gt;       return super().setup()
               ^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/pytest_asyncio/plugin.py:458: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
.venv/lib/python3.13/site-packages/pytest_asyncio/plugin.py:730: in pytest_fixture_setup
    return (yield)
            ^^^^^
tests/conftest.py:246: in require_tooling
    _require_tooling()
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.serving.test_semantic_mcp_tools" name="test_mcp_query_response_has_result_data[asyncio]" time="0.008"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">self = &lt;Coroutine test_mcp_query_response_has_result_data[asyncio]&gt;

    def setup(self) -&gt; None:
        runner_fixture_id = f"_{self._loop_scope}_scoped_runner"
        if runner_fixture_id not in self.fixturenames:
            self.fixturenames.append(runner_fixture_id)
&gt;       return super().setup()
               ^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/pytest_asyncio/plugin.py:458: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
.venv/lib/python3.13/site-packages/pytest_asyncio/plugin.py:730: in pytest_fixture_setup
    return (yield)
            ^^^^^
tests/conftest.py:246: in require_tooling
    _require_tooling()
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.serving.test_semantic_mcp_tools" name="test_mcp_typed_response_structure[asyncio]" time="0.006"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">self = &lt;Coroutine test_mcp_typed_response_structure[asyncio]&gt;

    def setup(self) -&gt; None:
        runner_fixture_id = f"_{self._loop_scope}_scoped_runner"
        if runner_fixture_id not in self.fixturenames:
            self.fixturenames.append(runner_fixture_id)
&gt;       return super().setup()
               ^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/pytest_asyncio/plugin.py:458: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
.venv/lib/python3.13/site-packages/pytest_asyncio/plugin.py:730: in pytest_fixture_setup
    return (yield)
            ^^^^^
tests/conftest.py:246: in require_tooling
    _require_tooling()
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.serving.test_semantic_mcp_tools" name="test_mcp_tool_error_handling[asyncio]" time="0.010"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">self = &lt;Coroutine test_mcp_tool_error_handling[asyncio]&gt;

    def setup(self) -&gt; None:
        runner_fixture_id = f"_{self._loop_scope}_scoped_runner"
        if runner_fixture_id not in self.fixturenames:
            self.fixturenames.append(runner_fixture_id)
&gt;       return super().setup()
               ^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/pytest_asyncio/plugin.py:458: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
.venv/lib/python3.13/site-packages/pytest_asyncio/plugin.py:730: in pytest_fixture_setup
    return (yield)
            ^^^^^
tests/conftest.py:246: in require_tooling
    _require_tooling()
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.serving.mcp.test_resources" name="test_resource_store_creates_directory" time="0.001"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.serving.mcp.test_resources" name="test_resource_store_get_unknown_token" time="0.002"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.serving.mcp.test_resources" name="test_resource_store_put_and_get_jsonl" time="0.001"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.serving.mcp.test_resources" name="test_resource_store_jsonl_stream_encoding_is_utf8" time="0.001"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.serving.mcp.test_resources" name="test_resource_store_jsonl_encoding_is_utf8" time="0.003"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.serving.mcp.test_meta_sql_resources" name="test_mcp_meta_views_sql_rejects_unsafe_sql[asyncio]" time="0.001"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">self = &lt;Coroutine test_mcp_meta_views_sql_rejects_unsafe_sql[asyncio]&gt;

    def setup(self) -&gt; None:
        runner_fixture_id = f"_{self._loop_scope}_scoped_runner"
        if runner_fixture_id not in self.fixturenames:
            self.fixturenames.append(runner_fixture_id)
&gt;       return super().setup()
               ^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/pytest_asyncio/plugin.py:458: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/conftest.py:246: in require_tooling
    _require_tooling()
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.serving.mcp.test_resources" name="test_resource_store_get_meta" time="0.000"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.serving.mcp.test_resources" name="test_resource_store_put_and_get_json" time="0.001"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.serving.mcp.test_sampling" name="test_mcp_sampling_disabled_does_not_sample[asyncio]" time="0.001"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">self = &lt;Coroutine test_mcp_sampling_disabled_does_not_sample[asyncio]&gt;

    def setup(self) -&gt; None:
        runner_fixture_id = f"_{self._loop_scope}_scoped_runner"
        if runner_fixture_id not in self.fixturenames:
            self.fixturenames.append(runner_fixture_id)
&gt;       return super().setup()
               ^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/pytest_asyncio/plugin.py:458: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/conftest.py:246: in require_tooling
    _require_tooling()
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.serving.mcp.test_resources" name="test_resource_store_get_meta_not_found" time="0.000"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.serving.mcp.test_resources" name="test_stored_metadata_is_frozen" time="0.004"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.serving.mcp.test_resources" name="test_resource_store_get_preview" time="0.001"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.serving.mcp.test_sampling" name="test_mcp_sampling_opt_in_adds_summary[asyncio]" time="0.001"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">self = &lt;Coroutine test_mcp_sampling_opt_in_adds_summary[asyncio]&gt;

    def setup(self) -&gt; None:
        runner_fixture_id = f"_{self._loop_scope}_scoped_runner"
        if runner_fixture_id not in self.fixturenames:
            self.fixturenames.append(runner_fixture_id)
&gt;       return super().setup()
               ^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/pytest_asyncio/plugin.py:458: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/conftest.py:246: in require_tooling
    _require_tooling()
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.serving.mcp.test_meta_sql_resources" name="test_mcp_meta_views_sql_resources_round_trip[asyncio]" time="0.001"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">self = &lt;Coroutine test_mcp_meta_views_sql_resources_round_trip[asyncio]&gt;

    def setup(self) -&gt; None:
        runner_fixture_id = f"_{self._loop_scope}_scoped_runner"
        if runner_fixture_id not in self.fixturenames:
            self.fixturenames.append(runner_fixture_id)
&gt;       return super().setup()
               ^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/pytest_asyncio/plugin.py:458: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/conftest.py:246: in require_tooling
    _require_tooling()
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.serving.mcp.test_resources" name="test_resource_store_get_preview_small_dataset" time="0.001"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.serving.mcp.test_resources" name="test_mcp_semantic_export_returns_uri[asyncio]" time="0.000"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">self = &lt;Coroutine test_mcp_semantic_export_returns_uri[asyncio]&gt;

    def setup(self) -&gt; None:
        runner_fixture_id = f"_{self._loop_scope}_scoped_runner"
        if runner_fixture_id not in self.fixturenames:
            self.fixturenames.append(runner_fixture_id)
&gt;       return super().setup()
               ^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/pytest_asyncio/plugin.py:458: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/conftest.py:246: in require_tooling
    _require_tooling()
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.serving.mcp.test_resources" name="test_resource_store_put_with_metadata" time="0.007"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.serving.mcp.test_resources" name="test_mcp_semantic_export_with_filters[asyncio]" time="0.001"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">self = &lt;Coroutine test_mcp_semantic_export_with_filters[asyncio]&gt;

    def setup(self) -&gt; None:
        runner_fixture_id = f"_{self._loop_scope}_scoped_runner"
        if runner_fixture_id not in self.fixturenames:
            self.fixturenames.append(runner_fixture_id)
&gt;       return super().setup()
               ^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/pytest_asyncio/plugin.py:458: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/conftest.py:246: in require_tooling
    _require_tooling()
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.serving.mcp.test_resources" name="test_stored_artifact_is_frozen" time="0.001"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.serving.mcp.test_resources" name="test_mcp_semantic_export_error_invalid_view[asyncio]" time="0.001"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">self = &lt;Coroutine test_mcp_semantic_export_error_invalid_view[asyncio]&gt;

    def setup(self) -&gt; None:
        runner_fixture_id = f"_{self._loop_scope}_scoped_runner"
        if runner_fixture_id not in self.fixturenames:
            self.fixturenames.append(runner_fixture_id)
&gt;       return super().setup()
               ^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/pytest_asyncio/plugin.py:458: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/conftest.py:246: in require_tooling
    _require_tooling()
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.serving.mcp.test_resources" name="test_mcp_semantic_export_json_format[asyncio]" time="0.000"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">self = &lt;Coroutine test_mcp_semantic_export_json_format[asyncio]&gt;

    def setup(self) -&gt; None:
        runner_fixture_id = f"_{self._loop_scope}_scoped_runner"
        if runner_fixture_id not in self.fixturenames:
            self.fixturenames.append(runner_fixture_id)
&gt;       return super().setup()
               ^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/pytest_asyncio/plugin.py:458: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/conftest.py:246: in require_tooling
    _require_tooling()
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.serving.mcp.test_resources" name="test_mcp_resource_read_semantic_views[asyncio]" time="0.001"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">self = &lt;Coroutine test_mcp_resource_read_semantic_views[asyncio]&gt;

    def setup(self) -&gt; None:
        runner_fixture_id = f"_{self._loop_scope}_scoped_runner"
        if runner_fixture_id not in self.fixturenames:
            self.fixturenames.append(runner_fixture_id)
&gt;       return super().setup()
               ^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/pytest_asyncio/plugin.py:458: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/conftest.py:246: in require_tooling
    _require_tooling()
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.serving.mcp.test_resources" name="test_mcp_static_resources_available[asyncio]" time="0.000"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">self = &lt;Coroutine test_mcp_static_resources_available[asyncio]&gt;

    def setup(self) -&gt; None:
        runner_fixture_id = f"_{self._loop_scope}_scoped_runner"
        if runner_fixture_id not in self.fixturenames:
            self.fixturenames.append(runner_fixture_id)
&gt;       return super().setup()
               ^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/pytest_asyncio/plugin.py:458: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/conftest.py:246: in require_tooling
    _require_tooling()
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.serving.mcp.test_resources" name="test_mcp_resource_meta_resources[asyncio]" time="0.001"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">self = &lt;Coroutine test_mcp_resource_meta_resources[asyncio]&gt;

    def setup(self) -&gt; None:
        runner_fixture_id = f"_{self._loop_scope}_scoped_runner"
        if runner_fixture_id not in self.fixturenames:
            self.fixturenames.append(runner_fixture_id)
&gt;       return super().setup()
               ^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/pytest_asyncio/plugin.py:458: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/conftest.py:246: in require_tooling
    _require_tooling()
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.serving.mcp.test_resources" name="test_mcp_resource_export_meta[asyncio]" time="0.001"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">self = &lt;Coroutine test_mcp_resource_export_meta[asyncio]&gt;

    def setup(self) -&gt; None:
        runner_fixture_id = f"_{self._loop_scope}_scoped_runner"
        if runner_fixture_id not in self.fixturenames:
            self.fixturenames.append(runner_fixture_id)
&gt;       return super().setup()
               ^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/pytest_asyncio/plugin.py:458: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/conftest.py:246: in require_tooling
    _require_tooling()
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.serving.mcp.test_sql_fingerprint" name="test_mcp_sql_fingerprint_is_stable_for_same_request[asyncio]" time="0.000"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">self = &lt;Coroutine test_mcp_sql_fingerprint_is_stable_for_same_request[asyncio]&gt;

    def setup(self) -&gt; None:
        runner_fixture_id = f"_{self._loop_scope}_scoped_runner"
        if runner_fixture_id not in self.fixturenames:
            self.fixturenames.append(runner_fixture_id)
&gt;       return super().setup()
               ^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/pytest_asyncio/plugin.py:458: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/conftest.py:246: in require_tooling
    _require_tooling()
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.serving.mcp.test_resources" name="test_mcp_resource_export_bytes_chunk_for_arrow[asyncio]" time="0.001"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">self = &lt;Coroutine test_mcp_resource_export_bytes_chunk_for_arrow[asyncio]&gt;

    def setup(self) -&gt; None:
        runner_fixture_id = f"_{self._loop_scope}_scoped_runner"
        if runner_fixture_id not in self.fixturenames:
            self.fixturenames.append(runner_fixture_id)
&gt;       return super().setup()
               ^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/pytest_asyncio/plugin.py:458: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/conftest.py:246: in require_tooling
    _require_tooling()
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.serving.mcp.test_resources" name="test_mcp_resource_read_meta_serving[asyncio]" time="0.001"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">self = &lt;Coroutine test_mcp_resource_read_meta_serving[asyncio]&gt;

    def setup(self) -&gt; None:
        runner_fixture_id = f"_{self._loop_scope}_scoped_runner"
        if runner_fixture_id not in self.fixturenames:
            self.fixturenames.append(runner_fixture_id)
&gt;       return super().setup()
               ^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/pytest_asyncio/plugin.py:458: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/conftest.py:246: in require_tooling
    _require_tooling()
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.serving.mcp.test_resources" name="test_mcp_resource_meta_environment[asyncio]" time="0.001"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">self = &lt;Coroutine test_mcp_resource_meta_environment[asyncio]&gt;

    def setup(self) -&gt; None:
        runner_fixture_id = f"_{self._loop_scope}_scoped_runner"
        if runner_fixture_id not in self.fixturenames:
            self.fixturenames.append(runner_fixture_id)
&gt;       return super().setup()
               ^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/pytest_asyncio/plugin.py:458: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/conftest.py:246: in require_tooling
    _require_tooling()
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.serving.mcp.test_resources" name="test_mcp_export_tool_has_annotations[asyncio]" time="0.000"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">self = &lt;Coroutine test_mcp_export_tool_has_annotations[asyncio]&gt;

    def setup(self) -&gt; None:
        runner_fixture_id = f"_{self._loop_scope}_scoped_runner"
        if runner_fixture_id not in self.fixturenames:
            self.fixturenames.append(runner_fixture_id)
&gt;       return super().setup()
               ^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/pytest_asyncio/plugin.py:458: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/conftest.py:246: in require_tooling
    _require_tooling()
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.serving.mcp.test_resources" name="test_mcp_resource_export_preview[asyncio]" time="0.001"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">self = &lt;Coroutine test_mcp_resource_export_preview[asyncio]&gt;

    def setup(self) -&gt; None:
        runner_fixture_id = f"_{self._loop_scope}_scoped_runner"
        if runner_fixture_id not in self.fixturenames:
            self.fixturenames.append(runner_fixture_id)
&gt;       return super().setup()
               ^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/pytest_asyncio/plugin.py:458: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/conftest.py:246: in require_tooling
    _require_tooling()
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.serving.mcp.test_resources" name="test_mcp_resource_export_sql[asyncio]" time="0.001"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">self = &lt;Coroutine test_mcp_resource_export_sql[asyncio]&gt;

    def setup(self) -&gt; None:
        runner_fixture_id = f"_{self._loop_scope}_scoped_runner"
        if runner_fixture_id not in self.fixturenames:
            self.fixturenames.append(runner_fixture_id)
&gt;       return super().setup()
               ^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/pytest_asyncio/plugin.py:458: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/conftest.py:246: in require_tooling
    _require_tooling()
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.serving.mcp.test_resources" name="test_mcp_resource_export_lines_chunk[asyncio]" time="0.001"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">self = &lt;Coroutine test_mcp_resource_export_lines_chunk[asyncio]&gt;

    def setup(self) -&gt; None:
        runner_fixture_id = f"_{self._loop_scope}_scoped_runner"
        if runner_fixture_id not in self.fixturenames:
            self.fixturenames.append(runner_fixture_id)
&gt;       return super().setup()
               ^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/pytest_asyncio/plugin.py:458: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/conftest.py:246: in require_tooling
    _require_tooling()
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.serving.mcp.test_tasks" name="test_mcp_export_task_cancellation_cleans_up_artifacts[asyncio]" time="0.001"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">self = &lt;Coroutine test_mcp_export_task_cancellation_cleans_up_artifacts[asyncio]&gt;

    def setup(self) -&gt; None:
        runner_fixture_id = f"_{self._loop_scope}_scoped_runner"
        if runner_fixture_id not in self.fixturenames:
            self.fixturenames.append(runner_fixture_id)
&gt;       return super().setup()
               ^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/pytest_asyncio/plugin.py:458: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/conftest.py:246: in require_tooling
    _require_tooling()
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.serving.mcp.test_prompts_advanced" name="test_mcp_list_prompts_includes_tags_and_version_meta[asyncio]" time="0.001"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">self = &lt;Coroutine test_mcp_list_prompts_includes_tags_and_version_meta[asyncio]&gt;

    def setup(self) -&gt; None:
        runner_fixture_id = f"_{self._loop_scope}_scoped_runner"
        if runner_fixture_id not in self.fixturenames:
            self.fixturenames.append(runner_fixture_id)
&gt;       return super().setup()
               ^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/pytest_asyncio/plugin.py:458: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/conftest.py:246: in require_tooling
    _require_tooling()
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.serving.mcp.test_sql_fingerprint" name="test_mcp_sql_fingerprint_changes_when_limit_changes[asyncio]" time="0.000"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">self = &lt;Coroutine test_mcp_sql_fingerprint_changes_when_limit_changes[asyncio]&gt;

    def setup(self) -&gt; None:
        runner_fixture_id = f"_{self._loop_scope}_scoped_runner"
        if runner_fixture_id not in self.fixturenames:
            self.fixturenames.append(runner_fixture_id)
&gt;       return super().setup()
               ^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/pytest_asyncio/plugin.py:458: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/conftest.py:246: in require_tooling
    _require_tooling()
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.serving.mcp.test_prompts_advanced" name="test_mcp_get_prompt_wizard_query_view_uses_elicitation[asyncio]" time="0.002"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">self = &lt;Coroutine test_mcp_get_prompt_wizard_query_view_uses_elicitation[asyncio]&gt;

    def setup(self) -&gt; None:
        runner_fixture_id = f"_{self._loop_scope}_scoped_runner"
        if runner_fixture_id not in self.fixturenames:
            self.fixturenames.append(runner_fixture_id)
&gt;       return super().setup()
               ^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/pytest_asyncio/plugin.py:458: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/conftest.py:246: in require_tooling
    _require_tooling()
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_runner_plumbing" name="test_repo_scan_honors_scan_profile" time="0.001"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.serving.mcp.test_resources" name="test_mcp_resource_export_meta_row_count_for_parquet[asyncio]" time="0.000"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">self = &lt;Coroutine test_mcp_resource_export_meta_row_count_for_parquet[asyncio]&gt;

    def setup(self) -&gt; None:
        runner_fixture_id = f"_{self._loop_scope}_scoped_runner"
        if runner_fixture_id not in self.fixturenames:
            self.fixturenames.append(runner_fixture_id)
&gt;       return super().setup()
               ^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/pytest_asyncio/plugin.py:458: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/conftest.py:246: in require_tooling
    _require_tooling()
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_runner_plumbing" name="test_coverage_ingest_uses_runner" time="0.001"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_runner_plumbing" name="test_typing_ingest_uses_shared_runner" time="0.000"><skipped type="pytest.skip" message="Schema mismatch: StaticDiagnosticRow (6 cols) vs static_diagnostics table (8 cols)">/Users/paulheyse/CodeIntel/CodeIntel/tests/ingestion/test_runner_plumbing.py:126: Schema mismatch: StaticDiagnosticRow (6 cols) vs static_diagnostics table (8 cols)</skipped></testcase><testcase classname="tests.ingestion.test_runner_plumbing" name="test_tests_ingest_uses_report_file" time="0.001"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_pyright_plugin_not_found_downgrades_to_not_found_status" time="0.001"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_scip_ingest" name="test_scip_target_writes_tables" time="0.001"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_tool_runner_unknown_tool_raises_value_error" time="0.001"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.serving.mcp.test_middleware_logging_smoke" name="test_mcp_middleware_logging_smoke[asyncio]" time="0.000"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">self = &lt;Coroutine test_mcp_middleware_logging_smoke[asyncio]&gt;

    def setup(self) -&gt; None:
        runner_fixture_id = f"_{self._loop_scope}_scoped_runner"
        if runner_fixture_id not in self.fixturenames:
            self.fixturenames.append(runner_fixture_id)
&gt;       return super().setup()
               ^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/pytest_asyncio/plugin.py:458: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/conftest.py:246: in require_tooling
    _require_tooling()
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_tool_runner_missing_binary_raises_not_found_error" time="0.001"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_default_registry_contains_expected_plugins" time="0.002"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_diagnostic_result_errors_by_path" time="0.000"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_coverage_result_with_files" time="0.001"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_tool_service_pyrefly_parses_errors" time="0.002"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_tool_status_enum_values" time="0.000"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_diagnostic_entry_attributes" time="0.001"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.serving.mcp.test_tasks" name="test_mcp_export_task_mode_completes[asyncio]" time="0.001"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">self = &lt;Coroutine test_mcp_export_task_mode_completes[asyncio]&gt;

    def setup(self) -&gt; None:
        runner_fixture_id = f"_{self._loop_scope}_scoped_runner"
        if runner_fixture_id not in self.fixturenames:
            self.fixturenames.append(runner_fixture_id)
&gt;       return super().setup()
               ^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/pytest_asyncio/plugin.py:458: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/conftest.py:246: in require_tooling
    _require_tooling()
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_coverage_result_ok_status" time="0.001"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_tool_service_coverage_reports_normalization" time="0.000"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_port_scip_occurrence_required_fields_only" time="0.000"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_scip_symbol_defaults" time="0.000"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_diagnostic_result_failed_status" time="0.001"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_port_scip_document_defaults" time="0.000"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_pyright_plugin_successful_run_returns_ok_status" time="0.001"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_scip_symbol_attributes" time="0.001"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_port_scip_document_attributes" time="0.000"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_tool_service_pyright_parses_errors" time="0.002"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_test_case_attributes" time="0.001"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_scip_result_with_documents" time="0.001"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_test_case_defaults" time="0.000"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_port_scip_occurrence_attributes" time="0.002"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_test_case_with_failure" time="0.000"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_scip_result_ok_status" time="0.002"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_test_result_ok_status" time="0.001"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_test_result_failed_count" time="0.000"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_tool_result_diagnostic_workflow" time="0.000"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_tool_service_run_plugin_success" time="0.000"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_test_result_passed_count" time="0.000"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_tool_service_get_plugin_returns_registered_plugin" time="0.001"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_tool_service_run_plugin_raises_key_error_for_unknown" time="0.000"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_tool_service_run_ruff_not_found" time="0.001"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_tool_service_run_pyright_success" time="0.000"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_tool_service_run_pytest_skips_if_exists" time="0.001"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_test_result_with_tests" time="0.000"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_tool_service_run_pyright_not_found" time="0.001"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_tool_service_run_coverage_not_found" time="0.001"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_tool_service_run_scip_not_found" time="0.001"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_diagnostic_report_from_error_counts" time="0.001"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_tool_service_run_pyrefly_not_found" time="0.000"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_diagnostic_report_empty" time="0.000"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_diagnostic_report_errors_by_path" time="0.001"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_coverage_report_by_path" time="0.001"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_coverage_file_summary_properties" time="0.000"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_file_diagnostic_count_attributes" time="0.001"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_tool_service_run_pytest_raises_not_found" time="0.000"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_parse_test_duration_valid" time="0.001"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_test_report_from_test_entries" time="0.000"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_coverage_report_from_file_reports" time="0.001"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_parse_test_duration_missing" time="0.000"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_parse_test_markers_dict" time="0.000"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_parse_test_markers_list" time="0.001"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_test_report_from_entries_skips_empty_nodeid" time="0.000"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_parse_test_markers_empty" time="0.000"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_parse_scip_range_four_elements" time="0.001"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_results_scip_occurrence_attributes" time="0.000"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_test_report_empty" time="0.001"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_parse_scip_range_invalid" time="0.000"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_parse_scip_range_three_elements" time="0.000"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_parse_scip_occurrence_valid" time="0.002"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_parse_scip_occurrence_invalid_symbol" time="0.000"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_parse_scip_occurrence_invalid_range" time="0.000"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_results_scip_document_attributes" time="0.001"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_scip_index_result_from_json_documents" time="0.001"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_scip_index_result_empty" time="0.000"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_scip_plugin_not_found_during_scip_python" time="0.001"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_scip_plugin_type_error_on_invalid_rel_paths" time="0.001"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_scip_index_result_skips_invalid_docs" time="0.000"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_pytest_plugin_not_found" time="0.004"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_scip_plugin_type_error_on_invalid_target_dir" time="0.001"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_pytest_plugin_type_error_on_missing_json_report_path" time="0.000"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_scip_plugin_type_error_on_missing_output_scip" time="0.001"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_scip_plugin_type_error_on_missing_output_json" time="0.000"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_tool_service_run_ruff_execution_error" time="0.001"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_pytest_plugin_execution_error" time="0.001"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_tool_service_run_pyright_returns_errors_from_parsed_report" time="0.003"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_tool_service_run_coverage_report_with_data" time="0.000"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_tool_service_run_pyright_execution_error" time="0.000"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_tool_service_run_pytest_report_creates_file" time="0.001"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_tool_service_run_coverage_report_failure_returns_empty" time="0.000"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_tool_service_run_scip_full_not_found_raises" time="0.001"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_tool_service_run_pyrefly_success" time="0.001"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_tool_service_run_scip_full_execution_error" time="0.000"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_tool_service_run_pyrefly_failure_returns_empty" time="0.001"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.tools.test_tool_sandbox_integration" name="test_tool_runner_records_pytest_version[asyncio]" time="0.002"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">self = &lt;Coroutine test_tool_runner_records_pytest_version[asyncio]&gt;

    def setup(self) -&gt; None:
        runner_fixture_id = f"_{self._loop_scope}_scoped_runner"
        if runner_fixture_id not in self.fixturenames:
            self.fixturenames.append(runner_fixture_id)
&gt;       return super().setup()
               ^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/pytest_asyncio/plugin.py:458: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/conftest.py:246: in require_tooling
    _require_tooling()
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase><testcase classname="tests.ingestion.test_tools" name="test_tool_service_run_pytest_report_execution_error" time="0.001"><error message="failed on setup with &quot;RuntimeError: Missing tool binaries: scip='scip'&quot;">@pytest.fixture(scope="session", autouse=True)
    def require_tooling() -&gt; None:
        """Fail fast if required tool binaries are missing."""
&gt;       _require_tooling()

tests/conftest.py:246: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

tools_config = None

    def require_tooling(tools_config: ToolsConfig | None = None) -&gt; dict[ToolName, Path]:
        """Verify configured tool binaries exist and are executable.
    
        Parameters
        ----------
        tools_config
            Optional explicit ToolsConfig; defaults to ToolsConfig.default().
    
        Returns
        -------
        dict[ToolName, Path]
            Mapping of tool names to resolved executable paths.
    
        Raises
        ------
        RuntimeError
            If any required tool binary is missing or not executable.
        """
        config = tools_config or ToolsConfig.default()
        resolved: dict[ToolName, Path] = {}
        missing: list[str] = []
        for tool in ToolName:
            configured = config.resolve_path(tool)
            candidate = Path(configured)
            if candidate.is_file():
                resolved_path = candidate
            else:
                discovered = shutil.which(configured)
                resolved_path = Path(discovered) if discovered else None
            if resolved_path is None or not os.access(resolved_path, os.X_OK):
                missing.append(f"{tool.value}={configured!r}")
                continue
            resolved[tool] = resolved_path
        if missing:
            message = "Missing tool binaries: " + ", ".join(missing)
&gt;           raise RuntimeError(message)
E           RuntimeError: Missing tool binaries: scip='scip'

tests/_helpers/tooling_audit.py:89: RuntimeError</error></testcase></testsuite></testsuites>